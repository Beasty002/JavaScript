const num = 5;
let result;
// if (num >= 50) {
//     result = "not fifty";
// }
// else {
//     result = "it could be worse"
// }

//can be written as

// condition ? if value(true) : else value (false)


result = num >= 50 ? "not fifty" : "it could be worse"

console.log(result);